#ifndef ARRAYQUEUE_H
#define ARRAYQUEUE_H

/* A function prototype that returns nothing and takes a pointer to a Queue. */
void newArrayQueue(Queue*);

/* A function prototype that returns nothing and takes a pointer to a Queue. */
void deleteArrayQueue(Queue*);

#endif
